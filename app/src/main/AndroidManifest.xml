<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fcp.reportebasuras">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logoappcap"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logoappcap"
        android:supportsRtl="true"
        android:hardwareAccelerated="false"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation"
            android:screenOrientation="locked"
            android:windowSoftInputMode="adjustPan|adjustResize"
            android:name=".PrincipalAdmin"
            android:label="@string/title_activity_principal_admin"
            android:theme="@style/AppTheme.NoActionBar">
            <!--intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter-->
        </activity>
        <activity android:name=".Registro" android:windowSoftInputMode="adjustPan|adjustResize" />
        <activity android:name=".Terminos" android:windowSoftInputMode="adjustPan|adjustResize" />
        <activity android:name=".NuevoReporte" android:windowSoftInputMode="adjustPan|adjustResize" />
        <activity android:name=".Principal" android:windowSoftInputMode="adjustPan|adjustResize" />
        <activity android:name=".Splash" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>